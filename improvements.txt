- Prevent non-alphanumeric characters in username.
- Randomize user and game ID’s. Possibly do the same for rounds and moves, but those are move numbers, sooo….

- Remove games created by player that are generated when looking for games.
	(Validation already exists that should prevent players from joining the
- Move code to generate a String response of HTML to its own class.
- Delete from timeout map when deleting game (after timeout has passed).


- Possible TODO: retrieve information about games if application crash

- Add auto-redirect for create game.

- Get Hosting fixed tomorrow.